apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: postgresql-ips-pvc
  name: postgresql-ips-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
status: {}